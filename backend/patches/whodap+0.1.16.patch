diff --git a/whodap/client.py b/whodap/client.py
index 28e27c5..dd1879a 100644
--- a/whodap/client.py
+++ b/whodap/client.py
@@ -214,6 +214,11 @@ class RDAPClient:
                 return None
             else:
                 raise
+        except MalformedQueryError:
+            if depth != 0:
+                return None
+            else:
+                raise
         # check for more authoritative source
         try:
             # If for some reason the response is invalid json, then just return None.
